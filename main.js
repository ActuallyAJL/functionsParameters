const quarterValue = (arg) => {
    let quartVal = arg/4;
    return quartVal;
}

// Invoke the function and store the return value
let argu = 200;
let quarter = quarterValue(argu);

// Log the return value
console.log(quarter);